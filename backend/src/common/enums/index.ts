export const enum VipLevel {
  NORMAL = 'NORMAL',
  VIP = 'VIP',
  SVIP = 'SVIP',
}

export const enum OrderStatus {
  PENDING = 'PENDING',
  PAID = 'PAID',
  CANCELLED = 'CANCELLED',
  REFUNDED = 'REFUNDED',
}

export const enum TransactionType {
  EARN = 'EARN',
  SPEND = 'SPEND',
  REFUND = 'REFUND',
}

export const enum FavoriteType {
  IMAGE = 'image',
  PROMPT = 'prompt',
}
